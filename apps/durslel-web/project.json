{
  "name": "durslel-web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/durslel-web",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:durslel"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "npx next build"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "npx next dev"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "apps/durslel-web/**/*.ts",
          "apps/durslel-web/**/*.tsx"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": {
        "jestConfig": "apps/durslel-web/jest.config.ts"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuZHVyc2xlbC5jb20k npx next build && npx opennextjs-cloudflare build --skipNextBuild && printf 'export const production = {};\\nexport const development = {};\\nexport const test = {};\\n' > .open-next/cloudflare/next-env.mjs && npx opennextjs-cloudflare deploy -- --env production"
      }
    },
    "deploy-test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YXJ0aXN0aWMtZmFsY29uLTcwNTcuY2xlcmsuYWNjb3VudHMuZGV2JA NEXT_PUBLIC_SERVICE_URL=https://dursel-service-test.ariuntuguldur3.workers.dev/graphql npx next build && npx opennextjs-cloudflare build --skipNextBuild && printf 'export const production = {};\\nexport const development = {};\\nexport const test = {};\\n' > .open-next/cloudflare/next-env.mjs && npx opennextjs-cloudflare deploy -- --env test"
      }
    },
    "deploy-renderer": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "npx wrangler deploy -c wrangler.renderer.jsonc --env production"
      }
    },
    "deploy-renderer-test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/durslel-web",
        "command": "npx wrangler deploy -c wrangler.renderer.jsonc --env test"
      }
    }
  }
}
