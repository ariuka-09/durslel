node_modules
dist
tmp
coverage
.nx

# next
.next/
out/
next-env.d.ts
.open-next/

# env — .env.production is committed on purpose (public Clerk key, needed at image build)
.env*
!.env.production
.dev.vars

# wrangler
.wrangler/

# rendered jobs
renders/

*.tsbuildinfo
.DS_Store

# wrangler bulk-secret payload — real values, never committed
secrets.json
